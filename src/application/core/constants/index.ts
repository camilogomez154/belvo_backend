import * as Repositories from '../../repositories';
import * as UseCases from '../../useCases';

export const respositories = Object.values(Repositories);
export const useCases = Object.values(UseCases);
